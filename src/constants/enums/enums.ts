export enum SupportedPages {
  Home = '/',
  Curiosity = '/curiosity',
  Opportunity = '/opportunity',
  Spirit = '/spirit',
  NotFound = '*',
}

export enum SupportedColors {
  Primary = 'primary',
  Default = 'default',
}

export enum SupportedSizes {
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
  XXL = 'xxl',
}

export enum SupportedWeights {
  Low = 'low',
  Medium = 'medium',
  High = 'high',
}

export enum SupportedIconsAnimations {
  Spin = 'spin',
  Bounce = 'bounce',
  Flip = 'flip',
  Shake = 'shake',
}
