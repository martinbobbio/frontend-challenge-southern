// Styled Components
import { styled } from 'styled-components';
// Libreries
import { Chip } from '@mui/material';

export const FilterChipsStyled = styled.div``;

export const ChipStyled = styled(Chip)`
  margin-right: 16px;
`;
